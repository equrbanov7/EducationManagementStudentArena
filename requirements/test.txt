# ═══════════════════════════════════════════════════════════════════════════
# EMS ARENA - TEST DEPENDENCIES
# ═══════════════════════════════════════════════════════════════════════════
# Dependencies for running tests
# ═══════════════════════════════════════════════════════════════════════════

-r base.txt

# ═══════════════════════════════════════════════════════════════════════════
# DATABASE (PostgreSQL - binary wheel for fast test setup)
# ═══════════════════════════════════════════════════════════════════════════
psycopg2-binary==2.9.9

# ═══════════════════════════════════════════════════════════════════════════
# TESTING TOOLS
# ═══════════════════════════════════════════════════════════════════════════
# pytest 9.1+ məcburidir: unittest.subTest(obj=…) parametrləri xdist worker→
# nəzarətçi kanalında serializasiya olunmalıdır (pytest-dev/pytest-xdist#1273,
# 9.1-də saferepr ilə düzəldilib; 9.0.x-də DumpError verir).
pytest==9.1.1

# `tests/test_infra_compose_config.py` docker-compose YAML-ını parse edir
# (2026-09-13, Codex audit P2-08). Lokal venv-də PyYAML bandit-in asılılığı
# kimi gəlirdi; CI test mühitində bandit yoxdur → açıq pin.
PyYAML==6.0.3
# i18n kataloq testləri (`core/tests/test_i18n_source_scan.py`,
# `test_audit_2026_09_13_frontend.py`) `.po` fayllarını polib ilə oxuyur; lint.txt-də
# pinlənib, amma CI unit-test job-u yalnız test.txt quraşdırır (2026-09-13 audit).
polib==1.2.0
pytest-asyncio==1.3.0
pytest-django==4.12.0
pytest-cov==6.1.0
pytest-timeout==2.4.0
# CI paralelləşdirmə (2026-09-03): `-n 4 --dist loadfile` — hər worker öz
# test DB-sini (test_<db>_gwN) yaradır; bax .github/workflows/_unit-tests.yml.
pytest-xdist==3.8.0
