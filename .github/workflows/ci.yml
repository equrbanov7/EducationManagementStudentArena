# ═══════════════════════════════════════════════════════════════════════════
# EMS ARENA — CI/CD Pipeline (Orchestrator)
# ═══════════════════════════════════════════════════════════════════════════
# This file is the thin orchestrator that calls the individual reusable
# workflows defined in _lint.yml, _unit-tests.yml, _build.yml,
# _security.yml, _secret-scan.yml, _docker-build.yml, _container-scan.yml,
# _prod-smoke.yml, and _e2e-smoke.yml.
#
# Dependency graph:
#
#   lint ──┬──► unit-tests (3.11 + 3.12)
#          ├──► build
#          ├──► security
#          ├──► secret-scan
#          └──► docker-build ──┬──► container-scan
#                              ├──► prod-smoke
#                              └──► e2e-smoke
#
#   ci-success (waits for all of the above)
# ═══════════════════════════════════════════════════════════════════════════

name: 🚀 CI Pipeline

on:
  pull_request:
    branches:
      - main
      - Staging
      - Develop
    types:
      - opened
      - synchronize
      - reopened

  push:
    branches:
      - main
      - Staging
      - Develop

  workflow_dispatch:
    inputs:
      debug_enabled:
        type: boolean
        description: "Debug mode aktiv etmək"
        required: false
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# ─────────────────────────────────────────────────────────────────────────
# JOBS — each delegates to a focused reusable workflow
# ─────────────────────────────────────────────────────────────────────────
jobs:

  # ── 1. Code Quality / Lint ─────────────────────────────────────────────
  lint:
    uses: ./.github/workflows/_lint.yml
    with:
      python-version: "3.11"

  # ── 2a. Unit Tests (Python 3.11) ───────────────────────────────────────
  # Sahib 2026-09-21: push-larda (Develop/Staging/main) 3.11 job-u kritik yolda deyildi,
  # amma 30 dəq runner vaxtı yeyirdi; prod image 3.12-dir. PR/dispatch-da tam dəst + coverage.
  unit-tests-311:
    needs: lint
    if: github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
      pull-requests: write
    uses: ./.github/workflows/_unit-tests.yml
    with:
      python-version: "3.11"

  # ── 2b. Unit Tests (Python 3.12) ───────────────────────────────────────
  # 8 paralel shard (hər biri öz Postgres/Redis-i ilə; .test_durations ilə balanslı), coverage yox.
  unit-tests-312:
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        shard: ["1", "2", "3", "4", "5", "6", "7", "8"]
    uses: ./.github/workflows/_unit-tests.yml
    with:
      python-version: "3.12"
      shard: ${{ matrix.shard }}
      shards: "8"
      coverage: false

  # ── 2c. RLS Transaction-Pooling Gate (FAZA 4 / Task 3) ─────────────────
  # `pytest -m postgres` yalnız `RLS_TRANSACTION_SCOPED=True` altında —
  # tenant izolyasiyasının transaction-pooling rejimində qorunmasını yoxlayır.
  # 2026-09-21: 2 shard (8–11 dəq → ~5 dəq); 3.12 — prod image ilə eyni.
  rls-txn-pool:
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        shard: ["1", "2"]
    uses: ./.github/workflows/_rls-txn-pool.yml
    with:
      python-version: "3.12"
      shard: ${{ matrix.shard }}
      shards: "2"

  # ── 3. Build Check (collectstatic + migration check) ───────────────────
  build:
    needs: lint
    uses: ./.github/workflows/_build.yml
    with:
      python-version: "3.11"

  # ── 4. Security Scan ───────────────────────────────────────────────────
  security:
    needs: lint
    uses: ./.github/workflows/_security.yml
    with:
      python-version: "3.11"

  # ── 5. Secret Scan (Gitleaks) ──────────────────────────────────────────
  secret-scan:
    needs: lint
    permissions:
      contents: read
      pull-requests: read
    uses: ./.github/workflows/_secret-scan.yml

  # ── 6. Docker Production Build ─────────────────────────────────────────
  # Sahib 2026-09-21: Docker build → Trivy → prod-smoke → e2e zənciri (~10 dəq) yalnız
  # main push-larında və PR-larda; Develop/Staging push-ları onu gözləmir.
  docker-build:
    needs: lint
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main'
    uses: ./.github/workflows/_docker-build.yml

  # ── 7. Container CVE Scan (Trivy) ──────────────────────────────────────
  container-scan:
    needs: docker-build
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: ./.github/workflows/_container-scan.yml

  # ── 8. Production Smoke Test ───────────────────────────────────────────
  prod-smoke:
    needs: docker-build
    uses: ./.github/workflows/_prod-smoke.yml

  # ── 9. E2E Smoke Tests (Playwright) ────────────────────────────────────
  e2e-smoke:
    needs: docker-build
    uses: ./.github/workflows/_e2e-smoke.yml
    with:
      python-version: "3.11"

  # ── Final gate — branch-protection check ───────────────────────────────
  ci-success:
    name: ✅ CI Success
    runs-on: ubuntu-latest
    needs:
      - lint
      - unit-tests-311
      - unit-tests-312
      - rls-txn-pool
      - build
      - security
      - secret-scan
      - docker-build
      - container-scan
      - prod-smoke
      - e2e-smoke
    if: always()

    steps:
      - name: 📋 Check all jobs status
        env:
          # Develop/Staging push-larında Docker zənciri qəsdən ötürülür (bax docker-build.if).
          DOCKER_CHAIN_OPTIONAL: ${{ (github.event_name == 'push' && github.ref != 'refs/heads/main') && '1' || '0' }}
        run: |
          echo "=== CI Pipeline Summary ==="
          echo "Lint:             ${{ needs.lint.result }}"
          echo "Unit Tests 3.11:  ${{ needs.unit-tests-311.result }}"
          echo "Unit Tests 3.12:  ${{ needs.unit-tests-312.result }}"
          echo "RLS Txn-Pool:     ${{ needs.rls-txn-pool.result }}"
          echo "Build:            ${{ needs.build.result }}"
          echo "Security:         ${{ needs.security.result }}"
          echo "Secret Scan:      ${{ needs.secret-scan.result }}"
          echo "Docker Build:     ${{ needs.docker-build.result }}"
          echo "Container Scan:   ${{ needs.container-scan.result }}"
          echo "Prod Smoke:       ${{ needs.prod-smoke.result }}"
          echo "E2E Smoke:        ${{ needs.e2e-smoke.result }}"
          echo "=========================="

          # Fail closed: cancelled/skipped/unknown are not successful gates.
          if [[ "${{ needs.lint.result }}" != "success" ]]; then
            echo "❌ Lint job did not succeed: ${{ needs.lint.result }}"
            exit 1
          fi

          # 3.12 (shard-lı) həmişə məcburidir; 3.11 push-larda qəsdən ötürülür (skipped).
          if [[ "${{ needs.unit-tests-312.result }}" != "success" ]]; then
            echo "❌ Unit Tests (3.12) did not succeed: ${{ needs.unit-tests-312.result }}"
            exit 1
          fi
          if [[ "${{ needs.unit-tests-311.result }}" != "success" && "${{ needs.unit-tests-311.result }}" != "skipped" ]]; then
            echo "❌ Unit Tests (3.11) failed: ${{ needs.unit-tests-311.result }}"
            exit 1
          fi

          if [[ "${{ needs.rls-txn-pool.result }}" != "success" ]]; then
            echo "❌ RLS Transaction-Pooling Gate did not succeed: ${{ needs.rls-txn-pool.result }}"
            exit 1
          fi

          if [[ "${{ needs.build.result }}" != "success" ]]; then
            echo "❌ Build job did not succeed: ${{ needs.build.result }}"
            exit 1
          fi

          if [[ "${{ needs.security.result }}" != "success" ]]; then
            echo "❌ Security scan did not succeed: ${{ needs.security.result }}"
            exit 1
          fi

          if [[ "${{ needs.secret-scan.result }}" != "success" ]]; then
            echo "❌ Secret Scan did not succeed: ${{ needs.secret-scan.result }}"
            exit 1
          fi

          # ── Docker-dependent jobs are BLOCKING (audit PROJ-P1 #5) ──────
          # Əvvəllər advisory idi: build olunmayan, kritik CVE-li, prod
          # settings-də açılmayan və ya E2E-si qırılan kod `main`-ə keçib
          # deploy oluna bilirdi. Artıq bunlar da merge/deploy gate-ini
          # bloklayır. Docker Hub keçici problemləri (rate-limit/timeout)
          # üçün həll — job-u YENİDƏN İŞƏ SALMAQ, ətrafından keçmək yox.
          if [[ "${{ needs.docker-build.result }}" != "success" && ! ( "${{ needs.docker-build.result }}" == "skipped" && "$DOCKER_CHAIN_OPTIONAL" == "1" ) ]]; then
            echo "❌ Docker Build did not succeed: ${{ needs.docker-build.result }}"
            exit 1
          fi

          if [[ "${{ needs.container-scan.result }}" != "success" && ! ( "${{ needs.container-scan.result }}" == "skipped" && "$DOCKER_CHAIN_OPTIONAL" == "1" ) ]]; then
            echo "❌ Container Scan (Trivy) did not succeed: ${{ needs.container-scan.result }}"
            exit 1
          fi

          if [[ "${{ needs.prod-smoke.result }}" != "success" && ! ( "${{ needs.prod-smoke.result }}" == "skipped" && "$DOCKER_CHAIN_OPTIONAL" == "1" ) ]]; then
            echo "❌ Prod Smoke Test did not succeed: ${{ needs.prod-smoke.result }}"
            exit 1
          fi

          if [[ "${{ needs.e2e-smoke.result }}" != "success" && ! ( "${{ needs.e2e-smoke.result }}" == "skipped" && "$DOCKER_CHAIN_OPTIONAL" == "1" ) ]]; then
            echo "❌ E2E Smoke Test did not succeed: ${{ needs.e2e-smoke.result }}"
            exit 1
          fi

          echo "✅ All jobs passed!"

  # ── 10. Production deploy (main only) ──────────────────────────────────
  # Runs ON the self-hosted GitHub Actions runner installed on the production
  # server (wcuserver). The job rsyncs the freshly-checked-out code into the
  # live app dir — preserving runtime data (.env, media/, TLS certs) via
  # scripts/deploy/rsync-excludes.txt — then runs the docker-compose deploy +
  # health gate (scripts/deploy/remote_deploy.sh). Because it executes locally
  # on the box, no SSH and no public IP are needed; the server's private LAN
  # address is irrelevant. Only fires on a push to `main`, after ci-success.
  deploy-production:
    needs: ci-success
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: [self-hosted]
    environment: production
    concurrency:
      group: production-deploy
      cancel-in-progress: false
    env:
      APP_DIR: /home/wcu/EducationManagementStudentArena
      # HEALTHCHECK_HOST/EDGE_PROXY_MODE serverin .env-indən oxunur (LAN deploy).
      DEPLOY_TIMEOUT_SECONDS: "300"
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🚢 Deploy (docker-compose, in place)
        run: |
          set -euo pipefail
          if [ ! -d "$APP_DIR" ]; then
            echo "APP_DIR ($APP_DIR) does not exist on the runner host." >&2
            exit 1
          fi
          # Mirror the repo into the live app dir; runtime data (.env, media/,
          # docker/nginx/certs/) is preserved via rsync-excludes.txt.
          rsync -a --delete --exclude-from scripts/deploy/rsync-excludes.txt ./ "$APP_DIR/"
          cd "$APP_DIR"
          bash scripts/deploy/remote_deploy.sh
