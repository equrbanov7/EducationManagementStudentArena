# Legacy miqrasiyası — təhvil hesabatı (2026-08-27)

**Kimə:** Codex (və layihəyə yenidən qoşulan hər kəsə)
**Kimdən:** Claude — 2026-08-25-dən bəri icraçı
**Vəziyyət:** lokal repetisiya mərhələsi; **production-a heç nə yazılmayıb**

Bu sənəd `STATUS.md` ledger-ini əvəz etmir — onu tamamlayır. Ledger «hansı
mərhələ hansı gate-dən keçdi» sualına cavab verir; bu sənəd isə **son iki
günün qərarlarını, onların səbəbini və qalan işi** izah edir.

---

## 1. Bir abzasda vəziyyət

Qərbi Kaspi Universitetinin köhnə MyEdu sistemi (MariaDB, 2.14 GB, 81 cədvəl,
9,044,531 sətir) yeni EMS Arena Django sisteminə köçürülür. Köçürmə **birbaşa
SQL kopyası deyil** — hər sətir audited kontraktdan keçir, qərarı append-only
ledger-ə möhürlənir və eyni mənbədən eyni nəticə bayt-bəbayt təkrarlanır
(`determinism_digest`). Hazırda 17 faza kodlanıb və lokal repetisiya bazasında
işləyir. Sonrakı addım real serverə birdəfəlik keçiddir.

---

## 2. Faza registri (17)

Sıra nömrəsi qəsdən seyrəkdir — aradakı boşluqlar gələcək fazalar üçündür.

| # | Faza | Mənbə → Hədəf |
|---|---|---|
| 10 | `academic_structure` | `departments`/`speciality`/`groups` → `OrgUnit` ağacı + `Program` |
| 12 | `academic_catalog` | `lessons`/`curricula`/`curricula_plan` → `Subject`/`Curriculum` |
| 20 | `identity_cohort` | `students` + `workers` → kimlik kohortu |
| 25 | `student_placement` | yerləşdirmə qərarı (törəmə, mənbə cədvəli yoxdur) |
| 26 | `worker_materialisation` | müəllim/işçi hesabları + kafedra scope-u |
| 28 | `sar_materialisation` | `StudentAcademicRecord` + hesab aktivasiyası |
| 32 | `journal_periods` (J0) | `semestr_jurnal` → `AcademicPeriod` |
| 34 | `journal_offerings` (J1) | `journals` → `CourseOffering` (**qrup-başına bölünür**) |
| 36 | `journal_enrollments` (J2) | `journals.students_id` → `Enrollment` |
| 38 | `journal_lessons` (J3) | `journals_dates_added_by_teacher` → `Lesson` |
| 40 | `journal_marks` (J4) | `journals_dates_points` → `LessonMark` |
| 42 | `journal_components` (J5) | `k1`/`k2`/`k3`/`si` → `ComponentScore` |
| 43 | `journal_entry_scores` (J5b) | tarixi giriş balı → arxiv komponenti |
| 44 | `journal_finals` (J6) | `im`/`im2` → `FinalGrade`/`ResitRecord` |
| 45 | `journal_selfwork` (J9) | `sillabus_serbest_is` → `SelfWorkTopic` |
| 46 | `journal_lock` (J7) | bitmiş semestrlərin jurnallarını kilidləyir |
| 48 | `journal_reconcile` (J8) | say balansı + `yekun` güzgüsü (heç nə yazmır) |

---

## 3. Son iki günün qərarları və SƏBƏBLƏRİ

Bunlar texniki zövq məsələsi deyil — hər biri ölçülmüş problemə cavabdır.

### 3.1 Qrup-başına jurnal bölgüsü
**Problem:** bir köhnə jurnal bütün qrupları birləşdirirdi; nəticədə UI-da
61 MB-lıq HTML səhifələr yaranırdı (>5000 xanalı 46 jurnal).
**Qərar (sahibin):** «hər qrupun əslində bir jurnalı olsun».
**Nəticə:** >5000 xanalı jurnal 46 → 8. Qalan 8-i bir köhnə fənnin birləşmə
klasteridir və UI səhifələmə tələb edir (backlog).
**Təsir:** bir jurnal `uniqid` çoxlu `CourseOffering`-ə açılır — hədəfdə
açılış sayının mənbə jurnal sayından çox olması **gözləniləndir**.

### 3.2 Arxiv üzvlüyü (məzun/qeyri-aktiv tələbələr)
**Problem:** mənbənin ~30 %-i köçmürdü — 2,603 qeyri-aktiv tələbə.
**Qərar:** məzunlar silinmir, «arxiv üzvlüyü» ilə köçür.
**Nəticə:** +49,540 qeydiyyat açıldı. Qəbul ili tapılmayanlar üçün
`ARCHIVE_FALLBACK_ADMISSION_YEAR = 1950` sentineli.

### 3.3 J5b — tarixi giriş balı arxiv komponenti
**Problem:** tələbələrin 16.2 %-i səhvən «kəsilmiş» görünürdü.
**Kök səbəb:** data düzgün köçmüşdü, **hesablama** fərqlənirdi (köhnə sistemin
düsturu ilə tətbiq etdikdə 89.8 % ±2 uyğunluq).
**Qərar (sahibin):** «köhnə datanı silmə, pozma, dəyişmə — hər necə
hesablanıbsa hesablanıbdır».
**Həll:** köhnə giriş balı GENERIC komponent kimi **qalıq** şəklində saxlanılır
(`residual = clamp(girish − Σkollokvium − checklist, 0, 50)`), çünki
`entry_score_for` kollokviumu onsuz da üstünə gəlir.
**Simulyasiya:** kəsr 742 → 287, kredit 5,729 → 7,835, ÜOMG 56.71 → 64.57.

### 3.4 «Qiymətləndirilməyib» statusu
106,870 yazılışın 23,382-si nə keçmiş, nə kəsilmişdi — heç bir qutuya
düşmürdü və rəqəmlər səssizcə itirdi. İndi ayrıca sayılır ki, imtahan mərkəzi
icazə ilə onları doldura bilsin.

### 3.5 Jurnalda təsdiq zənciri LƏĞV EDİLDİ
**Sahibin sözü:** «jurnal təsdiqə getməyi yoxdu, elə olmur bu — müəllim balı
yazır və bitir». Əvəzinə **RİM semestr sonunda fakültə-fakültə jurnalları
bağlayır**; bildiriş imtahan mərkəzinin kollokvium marşrutu ilə eyni dizayndadır.

### 3.6 İmtahan balını yalnız İmtahan Mərkəzi yazır
Yazılı və praktiki imtahanlar kağız üzərində/kodda keçir. Ona görə balı sistemə
İmtahan Mərkəzi əl ilə daxil edir (qrup seç → bir-bir yaz → şəkil/mətn əlavə et).
İcazə qəsdən `final_score.entry` adlanır — `exam.*` wildcard-ı onu ÖRTMƏSİN deyə.
Bal tam ədəddir; **dəyişmək olar, silmək olmaz**.

### 3.7 Sənədsiz dəyişiklik qadağandır
**Sahibin sözü:** «təqdimatsız heç nəyi dəyişmək olmasın». Jurnal və imtahan
nəticəsi ilə bağlı kritik dəyişikliklərdə **təqdimat (sənəd) yüklənməsi
məcburidir** — audited correction axını PDF + sarı işarə + tarixçə ilə.

### 3.8 RİM = İKT Rəhbəri
Ayrıca rol yaradılmadı. **Mövcud `ikt_rehber` rolu RİM-dir, yalnız görünən adı
dəyişdi.** Rəqəmsal İnkişaf Mərkəzi əsasnaməsi əsasında səlahiyyətləri var:
parol sıfırlama, ad/soyad/**ata adı** üzrə istifadəçi axtarışı, bloklama,
soft-delete, məlumat yeniləmə, icazə vermə.

### 3.9 Transkript tələbədən gizlədildi
`registrar.public.STUDENT_TRANSCRIPT_SELF_SERVICE = False` — **tək bayraq iki
qapını** idarə edir: bölmə siyahısı (birbaşa URL də bağlıdır) və PDF endpoint-i
(404). Əməkdaş yolu toxunulmayıb. Müraciət (ərizə) pəncərəsi qurulanda açılacaq.

---

## 4. Bu gecə (2026-08-27) düzəldilən infrastruktur problemləri

### 4.1 CI-nin təsadüfi qırılması — KÖK SƏBƏB
`_lint.yml` lint alətlərini **pinsiz** quraşdırırdı (`pip install black isort …`),
keş açarı isə `requirements/local.txt`-ə bağlı idi. isort 9.0.0 çıxan kimi
**heç nə dəyişməyən kod** üç ardıcıl run-da qırmızıya düşdü. Üç fərqli versiya
işlənirdi: CI 9.0.0, yerli 8.0.1, pin faylı 7.0.0.

**Həll:** `requirements/lint.txt` — lint zəncirinin tək mənbəyi. Yerli mühiti
CI ilə eyniləşdirmək üçün: `pip install -r requirements/lint.txt`.

### 4.2 Fixture tip uyğunsuzluğu — 15 dəqiqəlik geribildirim döngəsi
`legacy_int`/`legacy_flag` tip üzrə **fail-closed**-dur (mətn görsə partlayır).
Test fixture-ı real MyEdu sxemindəki tipləri güzgüləməlidir, əks halda
uyğunsuzluq yalnız 15 dəqiqəlik MariaDB dəstində üzə çıxır.
`journals.fenn_saati` və `journals_dates_points{,_archive}.sem_muh` belə idi.

**Alət:** `scripts/check_legacy_fixture_types.py` — bütün kontrakt sahələrini
real sxemlə saniyələr içində tutuşdurur, bilinən-zərərsiz fərqlər üçün
sübutlu istisna siyahısı var.

### 4.3 iCloud dublikatları
macOS sinxronizasiyası « 2» şəkilçili surətlər yaradıb commit etmişdi —
sənədlərdə iki fərqli həqiqət mənbəyi. 9 fayl silindi, 2-si (əsli itmiş, tək
nüsxə) adı düzəldilib saxlanıldı.

---

## 4.4 Köhnə giriş balı düsturu — datadan çıxarıldı

Sahib rəsmi düsturu verməmişdən əvvəl onu datanın özündən geri-mühəndislik
yolu ilə tapdıq (müstəqil çarpaz-yoxlama olsun deyə):

```
giriş = ROUND(davamiyyət + sərbəst_iş + seminar)          maksimum 50
  davamiyyət = 10 × (N − Q) / N        0…10   (N = xana sayı, Q = «qb» sayı)
  sərbəst_iş = «si» xanası             0…10
  seminar    = 3 × ORTA(k1,k2,k3 + gündəlik qiymətlər)    0…30
```

Sahibin dediyi «qayıb cədvəli» **sayla yox, FAİZLƏ** pillələnir: ≤5 % → 10 bal,
5-15 % → 9, 15-25 % → 8, hər 10 faiz bəndinə bir bal. Kəsr şərti `Q/N > 25 %`.
Güzəşt (`guzest_*`) girişə daxil deyil; üzrlü qayıb təsirsizdir.

**Sübut gücü:** `balvereqi_logs` cədvəlində köhnə sistemin ÇAP OLUNMUŞ bal
vərəqləri saxlanılır — 903,943 tələbə sətri. Düstur orada **97.24 % dəqiq,
100 % ±1** uyğunlaşır və 2023-2026 boyu sabitdir.

Detal: `docs/migration/LEGACY_GIRISH_FORMULA.md`, referans implementasiya
`legacy_girish_reference.py`.

## 4.5 ⚠️ Köhnə və yeni modelin ÜÇ STRUKTUR FƏRQİ

Düsturu yeni sistemin `entry_score_for`-u ilə tutuşdurduqda üç fərq çıxdı:

| | Köhnə (MyEdu) | Yeni (EMS Arena) |
|---|---|---|
| **Davamiyyət** | 50 balın **10-u** davamiyyətdən | Bal vermir — yalnız qadağan mexanizmi |
| **Seminar/kollokvium** | `3 × ORTA(qiymətlər)` | Qiymətlərin **CƏMİ** |
| **Sərbəst iş** | `si` xanasının **BALI** (0-10) | Təhvil verilmiş mövzuların **SAYI** |
| **Üzrlü qayıb** | Adi qayıb kimi sayılır | Ayrıca `excused` statusu (4,467 xana) |

⚠️ **Bu, köçürülmüş tarixi ballara TƏSİR ETMİR** — J5b köhnə `girish` dəyərini
olduğu kimi arxiv komponenti kimi saxlayır (sahibin «hər necə hesablanıbsa
hesablanıbdır» tələbi). Fərq yalnız **yeni semestrlərdə** özünü göstərəcək.

Detal və sahibə verilən suallar: `docs/migration/GIRIS_BALI_MODEL_FERQI.md`.

---

## 4.6 SON TAM REPETİSİYA — nəticələr (2026-08-27, 04:07–06:39)

```
status = succeeded · failure_code = "" · blocking_issue_count = 0
17 faza · determinism_digest = ce45c420f273…16c4
transform_version = rehearsal-identity-v1.3770759730bc
```

Hədəf baza: `emsarena_rehearsal_b90d8e9fc8ef`.

| Göstərici | Əvvəlki run | Bu run | Fərq |
|---|---|---|---|
| StudentAcademicRecord | 5,224 | **7,703** | +47 % |
| CourseOffering | 9,599 | **11,115** | +16 % |
| Enrollment | 106,870 | **148,020** | +38 % |
| Lesson | 243,923 | **293,070** | +20 % |
| LessonMark | 2,833,993 | **3,711,153** | +31 % |
| ComponentScore | 384,496 | **686,477** | +78 % |
| FinalGrade | 83,684 | **114,021** | +36 % |
| ResitRecord | 3,502 | **5,063** | +45 % |
| SelfWorkTopic | 0 | **69,404** | yeni (J9) |

**Uzlaşdırma nərdivanı** (`scripts/legacy_reconcile_report.py`):
«tələbə jurnalda aktiv deyil» səbəbi ilə itən xana **1,265,843 → 242,195**
düşdü — arxiv üzvlüyü düzəlişinin ölçülmüş təsiri.  İzahsız qalıq: təqvim
xanalarında +190,667, komponentlərdə +2,292, imtahanlarda +557 (cəmi mənbənin
~3.3 %-i); böyük hissəsi ötürülən dərs sətirlərinə bağlıdır
(`lesson_skipped` = 146,992 = 85,673 dublikat + 61,319 orphan).

**⚠️ Ölçülmüş itki — `legacy_journal_student_group_mismatch` = 3,862.**
`resolve_student` tələbənin BUGÜNKÜ qrupuna baxır; qrupu dəyişmiş tələbənin
köhnə jurnaldakı sətri fail-closed atlanır.  Bu, tələbənin öz tarixçəsidir —
qrup dəyişməsi onu silməməlidir.  **Sahibin qərarı lazımdır.**

## 4.7 UI QA — bütün rollarda (təzə data üzərində)

Rol süpürgəsi: 8 rolun hamısında **0 problem**.  Sahibin bütün tapıntıları
(sərbəst iş mövzuları və balı, seminar balları, dərs tipi, «Nəticələrim»,
təkrar fənlər, transkript, akademik qeydlərin sürəti, select dizaynı, otaq/korpus)
təzə data üzərində yoxlandı və işləyir.

QA-nın tapdığı iki YENİ problem:
1. **Düzəldildi** — `unit-exams` və `superadmin-org-inspector` şablonun AJAX
   siyahısında var idi, backend qeydiyyatında yox → dekan klikləyəndə 403.
   Uyğunluq testi əlavə olundu.
2. **Açıq** — ECTS tərəqqi qutusu «Toplanmış: 0» göstərir, halbuki əsl qeyd
   91 kredit deyir.  Səbəb: `get_credit_summary` `Enrollment.Status.COMPLETED`
   sayır, bu status isə **istehsal kodunda heç vaxt təyin olunmur**.  Yəni bu,
   köçürmədən ƏVVƏL də mövcud olan məhsul xətasıdır.

---

## 4.8 Determinizm dəsti — YAŞIL

`test_mariadb_source_integration` + `test_rehearsal_source_integration` →
**3 passed (44:39)**.  İki müstəqil tam repetisiya bayt-bəbayt eyni
`determinism_digest` verir; gecənin bütün dəyişikliklərindən sonra
təkrarlanabilirlik zəmanəti qorunub.

## 4.9 ⚠️ ƏSAS KƏŞF — köhnə sistemdə qayıb bloku YOX İDİ

Sahibin «tələbələrin çoxu kəsilmiş görünür» şikayətinin kök səbəbi tapıldı və
müstəqil təsdiqləndi (bax `DATA_VERIFICATION_2026_08_27.md` §4.3).

**Sübut 1 — sxemdə belə sütun yoxdur.**  Mənbədə `buraxil/block/qayib/eligib/
limit/allowed` naxışları üzrə axtarış yalnız `allowed_qb` cədvəlini verir, o da
**üzrlü qayıb icazəsidir** (student_id + tarix aralığı + sənəd faylı), blok deyil.

**Sübut 2 — `yekun.kesr` qiymət nəticəsidir.**

| Hal | Say |
|---|---|
| `kesr=1` cəmi | 2,385 |
| bunlardan **imtahana girib** (`imtahanda > 0`) | **2,111 (88.5 %)** |
| `imtahanda = 0` (bloka bənzəyən) | 274 (11.5 %) |

Kəsilənlərin orta imtahan balı **13.1** — yəni imtahana giriblər və kəsiliblər.

**Yeni sistem isə 13,578 blok yaradır — mənbədəki ən çox 274 hala qarşı ~50×.**
Üstəlik bunların 4,315-i (31.8 %) dərs saatının sabit 2 yazılmasından doğan
SAXTA blokdur (§4.6-dakı düzəliş siyahısı).

**Sahibin qərarı lazımdır:** 25 % qayıb bloku tarixi semestrlərə tətbiq
olunsunmu?  Tövsiyə: xeyr — köhnə sistemdə olmayan qaydanı geriyə tətbiq etmək
«hər necə hesablanıbsa hesablanıbdır» qaydasını pozur.  Yeni semestrlərdə qalsın.

---

## 5. Sahibdən gözlənilən qərarlar

1. **Giriş balı düsturu (ƏN VACİB).** Rəsmi düstur gözlənilir; yuxarıdakı
   §4.4 onun çarpaz-yoxlamasıdır. §4.5-dəki üç struktur fərqi üçün qərar
   lazımdır: yeni semestrlərdə köhnə model bərpa olunsun, yeni model qalsın,
   yoxsa təşkilat səviyyəsində seçilə bilən olsun? Qərar gələnə qədər
   `entry_score_for`-a TOXUNULMAMALIDIR.
2. `sillabus.ders_saati` kontrakta salınsınmı? (`fenn_saati` boş olan 18.7 %
   jurnal üçün yeganə çarpaz-yoxlamadır; `lesson_hours > 0` qayıb limitini
   idarə edir).
3. Sərbəst iş mövzularının nömrələnməsi mənbədəki kimi boşluqlu qalsın, yoxsa
   1-10 kimi normallaşsın?
4. 50+ mövzulu 15 sillabus 10 slota kəsilir — UI limiti yenidən baxılsınmı?
5. 2021/2022 imtahan nəticələri mənbədə yoxdur — səbəbi bilinmir.

---

## 6. Yoxlama resepti

```bash
# Lint zəncirini CI ilə eyniləşdir
pip install -r requirements/lint.txt

# Tam PG dəsti (RLS/trigger sübutu SQLite-da GÖRÜNMÜR — postgres şərtdir)
DATABASE_URL="postgres://…" pytest apps core -q --create-db --ignore=tests/e2e

# Mənbə-uyğunluq + determinizm (lokal MariaDB konteyneri tələb edir, ~17 dəq)
pytest apps/legacy_import/tests/test_mariadb_source_integration.py \
       apps/legacy_import/tests/test_rehearsal_source_integration.py

# Repetisiya nəticəsinə UI-dan bax
STAGING_POSTGRES_DB=<rehearsal_db> scripts/staging_inspect.sh serve
```

---

## 7. Qalıq risklər

| Risk | Vəziyyət |
|---|---|
| Giriş balı düsturu | Sahibin rəsmi düsturu gözlənilir — kod DƏYİŞDİRİLMƏYİB |
| 8 nəhəng jurnal | UI səhifələməsi lazımdır (bir fənnin birləşmə klasteri) |
| İki tərcüməsiz msgid (`qrup`/`semestr`) | AZ düzgün, digər dillərdə AZ fallback |
| `cabinet_modules.py::transcript` | İnert — bayraq açılanda avtomatik işə düşür |
| `--emit-report-only` RLS xətası | P2, açıqdır |
| 2021/2022 imtahan nəticələri | Mənbədə yoxdur, səbəb naməlum |

---

## 8. 2026-09-02 audit düzəlişləri

Müstəqil köçürmə auditi (`PHASE1_DATA_AUDIT.md`) üç P0 və dörd P1 qüsur tapdı.
Bu bölmə **qüsur → kök səbəb → kod düzəlişi → təmir əmri → gözlənilən rəqəm**
zəncirini və **serverdə icra sırasını** verir.

### 8.0 İki səviyyəli düzəliş — niyə belədir

Ledger sətirləri `run_id`-yə möhürlənib, `upsert_entity_map` eyni legacy açar
üçün fərqli derivation hash-ı `legacy_entity_identity_conflict` ilə rədd edir və
`transform_version` policy-dən törəyir. Yəni **artıq köçürülmüş hədəfdə fazanın
hədəflənmiş təkrar icrası mümkün deyil** — bu, sübutun toxunulmazlığı üçün
qəsdən belədir. Ona görə hər qüsur İKİ yerdə bağlanır:

1. **faza qaydası** düzəldilir → növbəti TAM repetisiya doğru olur;
2. **təmir əmri** artıq köçürülmüş hədəfdə qərarı auditli şəkildə geri alır.

Kod qaydası dəyişdiyi üçün `TRANSFORM_FAMILY` **`rehearsal-identity-v1` →
`rehearsal-identity-v2`** bumb edildi: köhnə və yeni möhür heç vaxt qarışa bilməz.

**Bütün təmir əmrlərinin ortaq qapıları** (`services/repair_support.py`):
`--dry-run` DEFAULT · yazmaq üçün `--apply` · `--organization <slug>` ·
`--limit` · `--actor` · bazada `emsarena.rehearsal_target='disposable'` markeri
yoxdursa icra **rədd olunur**, serverdə `--i-know-this-is-production` açıq
şəkildə verilməlidir · qərar cədvəli həmişə çap olunur · hər dəyişən sətir üçün
`core.audit.log_action` · **heç nə silinmir**, mövcud dəyər üzərinə yazılmır ·
idempotent (ikinci icra 0 dəyişiklik).

### 8.1 P0-1 — 2 291 cari tələbə səhvən «məzun» sayılıb

**Qüsur.** Aktiv tələbələrin ~30 %-i `access_state='archived'` + `alumni` rolu
alıb; `identity.user_access_is_login_blocked` onları bütün autentifikasiya
səthlərində bloklayır.

**Kök səbəb.** `services/rehearsal_sar_phase.py::SarMaterialisationPhase._decide`
arxiv qoluna İKİ şərtlə düşürdü: `students.azadedildi=1` (200 sətir) **və**
«qəbul ili heç bir mənbədən çıxmadı» (2 291 sətir). İkincisi məzunluq ölçüsü
deyil — qəbul ili yalnız ona görə tapılmır ki, tələbənin qrupunda
`groups.start_year='0000'` yazılıb (mənbədə belə **248 qrup**).

**Kod düzəlişi.** Arxivin yeganə şərti indi `azadedildi=1`-dir. Qəbul ili
bilinməyən, amma buraxılmamış sətir **aktiv tələbə** kimi materiallaşır:
`FALLBACK_ADMISSION_YEAR` (1950) sentinelini daşıyır və yeni
`legacy_sar_active_no_admission_year` (INFO) kodu ilə işarələnir. İl
UYDURULMUR — model NULL qəbul etmir, sentinel isə açıq sübutdur.

**Təmir əmri.** `legacy_repair_archive_status`
(`services/repair_archive.py` + `apps/accounts/services/identity_archive.py::
restore_archived_account`). Seçim qaydası **ledger sübutuna** əsaslanır:
profil arxivdədir **və** SAR issue-ları arasında
`legacy_sar_archived_no_admission_year` var **və** `legacy_sar_departed_student`
YOXDUR. `--require-activity` əlavə olaraq ən azı bir yazılış tələb edir;
`--fix-admission-year` (default QAPALI) qəbul ilini ən erkən yazılışın akademik
ilindən düzəldir.

`archived → active` keçidi 0016 migration-ının trigger qapısındadır (evidence
tələb olunur), ona görə keçid yalnız yeni `restore_archived_account` servisindən
mümkündür: mövcud `AccountActivationEvidence` sətri EYNİ tranzaksiyada təzələnir,
üzvlüyün rolu `alumni → student` olur (üzvlük sətri **silinmir**, əvvəlki rol
audit sətrində qalır), sonra profil `active` edilir.

### 8.2 P0-2 — 100 tələbə + 14 işçinin heç bir hesabı yoxdur

**Qüsur.** E-poçtu sınıq (85), boş (1) və ya iki legacy sətrində təkrarlanan
(28 = 14 cüt) insanlar hədəfdə **ümumiyyətlə yoxdur**; 12 belə işçiyə bağlı
62 legacy jurnal müəllimsizdir.

**Kök səbəb.** `services/account_cutover.py` e-poçtu kimlik açarı kimi işlədir:
`legacy_account_email_invalid` / `_blank` / `_duplicate_source` sətri staging-ə
buraxmır və dublikat halda **hər iki tərəfi** karantinə salır.

**Kod düzəlişi.** `services/rehearsal_identity_placeholder.py` (yeni):
e-poçt kimlik açarı deyil, əlaqə kanalıdır. MƏHZ e-poçt formasına görə bloklanan
sətir indi deterministik yer-tutucu ilə staged olunur —
`myedu.{tip}.{legacy_pk}@placeholder.invalid` (RFC 2606 rezerv TLD, poçt getməsi
mümkün deyil), `email_verified=False`. Toqquşma faktı itmir: orijinal qayda
kodları + `legacy_account_email_placeholder_synthesised` (WARNING) ledger-ə düşür,
`source_row_hash` isə xam e-poçtu digest etməyə davam edir. **Username tərəfli**
kolliziyalar (kimlik açarının özü) yenə karantində qalır.

**Təmir əmri.** `legacy_repair_missing_accounts` (`services/repair_accounts.py`).
Mənbə bağlantısı ŞƏRTDİR (ad/soyad uydurulmur). Hesab
`stage_imported_account` → `activate_staged_account` ilə qurulur (eyni qapılar),
sonra müəllimlər üçün mənbədəki `journals.teacher_id` uyğunluğuna görə
**müəllimsiz** `CourseOffering` sətirlərinə `instructor` yazılır.

⚠️ **Qalıq iş:** SAR / Enrollment / jurnal xanaları bu əmrlə YARADILMIR — onlar
faza zəncirinin (placement → sar → J1…J9) məhsuludur. Bu 100 tələbənin akademik
tarixçəsi hədəfə yalnız **düzəldilmiş identity fazası ilə növbəti tam
repetisiyada** düşür. Əmr onların kimliyini və girişini bərpa edir.

### 8.3 P0-3 — cari akademik dövr yoxdur

**Qüsur.** `AcademicPeriod.is_current` heç bir sətirdə True deyil və bugünkü
tarixi əhatə edən dövr yoxdur. `registrar.public.build_student_subjects_context`
`is_current=True` tapmayanda `-start_date` üzrə sonuncuya düşür — o isə
2025/2026 **Yay**-dır, ona görə «Fənlərim» boş görünür.

**Bu faza qüsuru DEYİL.** `journal_periods` (V9) qəsdən heç bir dövrü cari elan
etmir: cari dövr tenant-ın öz təqvim qərarıdır. Legacy bayraq itmir —
`legacy_journal_period_current_flag` INFO issue-su kimi ledger-dədir.

**Təmir əmri.** `legacy_repair_current_period` (`services/repair_periods.py`).
Qərar: `--period` açıq seçim → bugünü əhatə edən dövr → legacy `is_current`
bayrağı → ən son dövr. `--create-year 2026/2027` yeni tədris ilinin üç fəslini
köçürülmüş dövrlərlə eyni tarix reseptiylə yaradır (cari elan ETMİR).

**Ölçülmüş qərar əsası (klon, 13 dövr).** Bugün (2026-09-02) heç bir dövrün
içində deyil — 2025/2026 Yay 31 avqustda bitir, 2026/2027 Payız isə 15 sentyabrda
başlayır. Legacy bayraq **2025/2026 Yay**-ı göstərir, amma o semestrdə cəmi
**10 açılış** var; 2026/2027-də isə **0**. Real dolu semestrlər: 2025/2026 Payız
(1 574 açılış) və **2025/2026 Yaz (1 212 açılış)**. Ona görə cutover anında cari
dövr **2025/2026 Yaz** olmalıdır (legacy bayrağı KÖR-KORANƏ izləmək «Fənlərim»-i
boş qoyar), 2026/2027 Payız isə tədris ili rəsmən başlayanda cari elan edilir.

### 8.4 P1 — demoqrafiya və profil qrup nömrəsi

`birth_date` və `gender` klonda 0 %-dir, çünki `services/legacy_demographics.py`
və onun `student_placement`-dəki çağırışı **2026-08-30-da** əlavə olunub, klondakı
run isə 2026-08-27-dədir — kod qüsurlu deyil, nüsxə köhnədir. Növbəti tam
repetisiya onu özü doldurur.

`student_group_number` isə tamamilə hədəf daxilində həll olunur (`SAR.group.name`).

**Təmir əmri.** `legacy_repair_demographics` — default olaraq yalnız
`student_group_number`-i doldurur (mənbə lazım deyil); `--from-source` verilərsə
doğum tarixi/cinsi legacy MariaDB-dən oxuyur. Hər iki halda **yalnız BOŞ** hədəf
sahəsi doldurulur.

### 8.5 Düzəldilmədən sənədləşdirilən tapıntılar

| Tapıntı | Qərar |
|---|---|
| **P1-1** 12 457 bal + 19 116 qayıb xanası dərs slotu olmadığı üçün görünmür | J12 (`journal_lesson_recovery`, order 41) tam və testlidir, amma **yalnız tam repetisiyanın içində** işləyir. Ona görə ayrıca təmir əmri YAZILMADI: **cutover-dan əvvəl J12 daxil olmaqla təzə tam repetisiya icra edilməlidir.** |
| **P1-5** `FinalGrade.is_published` 114 021/114 021 `false` | Bu bayraq **vestigialdır** — tələbə görünüşünü idarə edən `AssessmentScheme.is_published`-dir və onu RİM jurnal bağlayanda qoyur. Kod dəyişikliyi lazım deyil. |
| **P1-2** 3 075 SAR fənn planı olmayan kurikuluma bağlıdır | Kurikulum bərpası ayrıca iş dilimidir (backlog). |
| **P2-11** `legacy_reconcile_report.py` §4A/§4B yalançı qırmızı | Alət qüsurudur (`app.current_org_id` GUC qoyulmur), backlog. |

### 8.6 Serverdə icra sırası (runbook)

> Bütün əmrlər `--organization myedu-univ` ilə və **məhz bu sıra** ilə icra
> olunur. Serverdə repetisiya markeri olmadığı üçün hər `--apply`-a
> `--i-know-this-is-production` əlavə edilir. **Hər addımdan əvvəl dry-run
> icra edin və rəqəmləri aşağıdakı gözlənilən dəyərlərlə tutuşdurun.**

```bash
# 0) miqrasiyalar — ŞƏRTDİR: accounts.0018 «archived → active» bərpa səthini
#    (append-only bərpa sübutu + SECURITY DEFINER funksiya) qurur; onsuz
#    addım 1 hər sətirdə accounts_activation_evidence_function_required verir.
python manage.py migrate

# 1) P0-1 — səhv arxivlənmiş tələbələr
python manage.py legacy_repair_archive_status --organization myedu-univ
python manage.py legacy_repair_archive_status --organization myedu-univ \
    --apply --i-know-this-is-production

# 2) P0-2 — hesabı olmayan insanlar (legacy MariaDB bağlantısı ŞƏRTDİR)
#    Mənbə DSN qəbul edilmir — yalnız opt-in settings/env:
export LEGACY_MARIADB_SOURCE_ATTEST_ENABLED=1
export LEGACY_MARIADB_SOURCE_HOST=<source host>       # lokal konteyner üçün 127.0.0.1
export LEGACY_MARIADB_SOURCE_PORT=<port>              # `docker port <container>` ilə OXUYUN
export LEGACY_MARIADB_SOURCE_USER=root
export LEGACY_MARIADB_SOURCE_PASSWORD="$LEGACY_SOURCE_PASSWORD"
export LEGACY_MARIADB_SOURCE_DATABASE=myedudb
# export LEGACY_MARIADB_SOURCE_LOCAL_DISPOSABLE=local-container-only   # YALNIZ lokal konteyner
#    ⚠️ Mənbə server `@@GLOBAL.read_only = 1` olmalıdır (fail-closed qapı:
#    `legacy_source_server_not_read_only`). Konteyner restart-dan sonra bayraq itir.
python manage.py legacy_repair_missing_accounts --organization myedu-univ --with-sar
python manage.py legacy_repair_missing_accounts --organization myedu-univ \
    --with-sar --apply --i-know-this-is-production
#    --with-sar (R-9): hesabla birlikdə akademik qeydi də qurur. ONSUZ tələbə
#    kabinetdə heç nə görmür (build_student_subjects_context SAR-sız boş qayıdır).

# 2b) R-5 — legacy otaq reyestri (dərs modalındakı KORPUS→OTAQ seçimi üçün);
#     eyni LEGACY_MARIADB_SOURCE_* dəyişənləri ilə işləyir.
python manage.py legacy_repair_rooms --organization myedu-univ --from-source
python manage.py legacy_repair_rooms --organization myedu-univ \
    --from-source --apply --i-know-this-is-production

# 3) P0-3 — cari akademik dövr
#    ⚠️ 2026/2027 açılışları HƏLƏ YOXDUR (0 offering). Ona görə cutover anında
#    cari dövr SON REAL semestr olmalıdır — əks halda «Fənlərim» yenə boş qalır.
python manage.py legacy_repair_current_period --organization myedu-univ
python manage.py legacy_repair_current_period --organization myedu-univ \
    --period "2025/2026 Yaz" --apply --i-know-this-is-production
#    Yeni tədris ili başlayanda (15 sentyabr, rektor + tədris şöbəsi qərarı):
python manage.py legacy_repair_current_period --organization myedu-univ \
    --create-year 2026/2027 --period "2026/2027 Payız" \
    --apply --i-know-this-is-production

# 4) P1 — demoqrafiya + profil qrup nömrəsi
python manage.py legacy_repair_demographics --organization myedu-univ
#    (addım 2-dəki LEGACY_MARIADB_SOURCE_* dəyişənləri hələ də ixrac olunmuş olmalıdır)
python manage.py legacy_repair_demographics --organization myedu-univ \
    --from-source --apply --i-know-this-is-production
```

### 8.7 Klonda ölçülmüş gözlənilən rəqəmlər

`myedu-univ` üzərində (QA klonu, 2026-09-02). Serverdə dry-run çıxışı bunlarla
tutuşdurulmalıdır; ciddi fərq varsa **dayanın** və səbəbi araşdırın.

| Addım | Gözlənilən dry-run | Tətbiqdən sonra |
|---|---|---|
| 1 · arxiv statusu | `arxivdə olan profil 2490` · `restore 2291` · `keep_archived 199` (hamısı `source_azadedildi`) | `archived` 2 490 → **199** · `active` 5 948 → **8 239** · `student` üzvlüyü 5 228 → **7 519** · `alumni` 2 490 → **199** · 2 291 audit sətri (`legacy_repair:archive_status`) · 2 291 bərpa sübutu · **ikinci icra: 0 dəyişiklik** |
| 2 · hesabsız insanlar | `hesabsız legacy sətri 114` · `yaradılacaq 114` (tələbə 100 + işçi 14) | `myedu.student.*` 7 716 → **7 816** · `myedu.worker.*` 715 → **729** · 114 yer-tutucu e-poçt (hamısı `email_verified=false`) · `student` üzvlüyü +100, `teacher` +14 · müəllimsiz açılış 1 203 → **1 168** (35 bağlandı) · `--with-sar` ilə `StudentAcademicRecord` 7 703 → **7 799** (96 yeni; qalan 4-ün qrupu mənbədə orphan-dır — SAR uydurulmur) · **ikinci icra: yaradılacaq 0, SAR yaradıldı 0, «onsuz da var» 114**. Mənbə bağlantısı yoxdursa əmr `legacy_repair_source_unavailable` ilə imtina edir — doğru davranışdır. |
| 2b · otaq reyestri | `mənbə otağı 158` · `yaradılacaq 158` | `exams.ExamRoom` 0 → **158** (hamısı aktiv) · 4 korpus (`1`,`2`,`3`,`5`) · dərs modalı kaskadı 158 otaq qaytarır · **ikinci icra: yaradılacaq 0**. Mövcud (əl ilə adlandırılmış) otağın adı/korpusu ÜSTÜNDƏN YAZILMIR. |
| 3 · cari dövr | `dövr sayı 13` · `bugünü əhatə edən dövr YOXDUR` · legacy bayraq → 2025/2026 Yay (cəmi 10 açılış) | `is_current` = **2025/2026 Yaz** (1 212 açılış / 19 033 yazılış) · `--create-year 2026/2027` üç fəsil əlavə edir |
| 4 · demoqrafiya | `qrup nömrəsi namizədi 7703` | `student_group_number` 0 → **7 703** · `--from-source` ilə `birth_date` 0 → **2 175**, `gender` 0 → **1 693** (2 397 sətir oxundu) · **ikinci icra: yazıldı 0** |

`birth_date` fərqi (mənbədə 2 252, hədəfdə 2 175) **qəsdəndir**: `legacy_birth_date`
təqvimə uyğun olmayan/kəsik dəyəri fail-closed NULL saxlayır və `MM/DD/YYYY`
təxmini etmir.

**Tam sübut cədvəli, SQL və mənbə bağlantısının iki tələsi (dəyişkən Docker portu +
`@@GLOBAL.read_only`):** `docs/audits/2026-09-02/PHASE1_MIGRATION_REPAIRS.md` §4.5–4.7.

### 8.8 Cutover-dan əvvəl hələ də tələb olunan

0. **R-5/R-9 qeydi:** hər ikisi mövcud faza məntiqini təkrar işlədir — `legacy_rooms`
   (J10) fazasının saf funksiyaları və `sar_materialisation` pilləkəni. Təzə tam
   repetisiya bu iki addımı onsuz da özü edir; təmir əmrləri yalnız ARTIQ
   köçürülmüş hədəf üçündür.
1. **Təzə TAM repetisiya** (düzəldilmiş identity + SAR fazaları, J12 daxil) —
   yalnız o zaman 100 tələbənin akademik tarixçəsi, 12 457 bal + 19 116 qayıb
   xanası və demoqrafiya hədəfə düşür. Təmir əmrləri bunu ƏVƏZ ETMİR.
2. Tədris ili rəsmən başlayanda cari dövrün 2026/2027 Payız-a keçirilməsi.
