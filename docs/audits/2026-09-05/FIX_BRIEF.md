# Düzəliş fazası — agent brifinqi (2026-09-05)

`QA_BRIEF.md`-dəki mühit və qaydalar eynilə keçərlidir. Bu fayl YALNIZ düzəliş edən agentlər üçündür.

## İş üsulu

1. Sənə bir **tapıntı klasteri** verilir (ISSUES.md-dəki ID-lər). Əvvəlcə tapıntını canlı serverdə (:8100) və ya testlə TƏKRARLA — təkrarlana bilmirsə bunu qeyd et və dayanma, növbətiyə keç.
2. **Öz worktree-ində işlə** (orkestrator yaradır, yolu promptda verilir). Kanonik tree-yə və serverə toxunma; serveri restart ETMƏ (orkestrator edir).
3. Düzəliş qaydaları: düzgün qatda düzəlt (frontend yamağı ilə backend problemini örtmə); mövcud abstraksiyaları təkrar istifadə et (`templates/partials/ems_ui/*`, `static/css/ems_ui/*`, `EMSReady`/`EMSDelegate`/`EMSCore.fetchJSON`, `core/ui/status_catalog.py`, `get_permission_scope`); yeni asılılıq, dublikat komponent, hardcoded rəng/rol adı, `style=`/inline `<script>` YOX; sessiz `except` YOX; icazə keçidi YOX.
4. Hər düzəliş üçün **red→green test** (mövcud test faylına və ya yeni `test_*.py`): sqlite ilə `DATABASE_URL="sqlite://" venv/bin/pytest <yol> -p no:cacheprovider -q --no-migrations`. JS/CSS dəyişikliyi üçün Playwright ilə canlı sübut mümkün deyil (server köhnə koddadır) — ona görə şablon render testi + JS-in sintaksis yoxlaması (`node --check` və ya `venv/bin/python -c "import esprima"` yoxdur → `node -e "new Function(require('fs').readFileSync(p,'utf8'))"`) + orkestratorun restart-sonrası re-test-i.
5. i18n: yeni istifadəçi mətni `{% trans "…" context "…" %}` / `pgettext` ilə; 4 dil kataloquna əlavə (`scripts/i18n_fill_*.py` nümunəsi ilə yeni skript `scripts/i18n_fill_qa_2026_09_05.py` — bir agent yazır, digərləri onun sonuna sətir ƏLAVƏ edir → konflikt olmasın deyə hər agent ÖZ faylını yazır: `scripts/i18n_fill_qa_<klaster>.py`) və `venv/bin/python scripts/check_i18n_catalogs.py`.
6. Qapılar (hamısı yaşıl olmalıdır): `venv/bin/black -l 120 <fayllar>` · `venv/bin/isort <fayllar>` · `venv/bin/flake8 <fayllar>` · `venv/bin/python scripts/check_module_size.py --check` · `DJANGO_SETTINGS_MODULE=config.settings.test DATABASE_URL="sqlite://" venv/bin/python manage.py makemigrations --check --dry-run` · `venv/bin/python manage.py check` (sqlite env ilə).
7. Commit: worktree-də `git add -A && git commit` (mesaj AZ dilində, `fix(<app>): …` və ya `feat(<app>): …`; sonda `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`). Bir klaster = bir və ya bir neçə kiçik commit. Push ETMƏ.
8. Yekun: StructuredOutput — hər ID üçün `fixed|partial|not_reproduced|deferred`, dəyişən fayllar, test adları, qalıq risk, orkestrator üçün re-test addımları (URL + rol + gözlənilən nəticə).

## Xüsusi qeydlər

- **Skript sırası (P1-1):** `templates/base.html` bölmə `<script src>`-lərini `{% block content %}` içində, `ems_ajax_init.js`-i sonra yükləyir. Layihə konvensiyası: partial skriptlərinə `defer`. Sistemli həll: `<head>`-də kiçik xarici `static/js/ems_early.js` — `window.EMSReady` və `window.EMSDelegate.on` üçün NÖVBƏ stub-u (çağırışları yığır), `ems_ajax_init.js` isə `window.EMSReady = window.EMSReady || ready` əvəzinə stub-u ƏVƏZ EDİB növbəni boşaldır. Üstəlik 21 partial-a `defer` əlavə et (siyahı ISSUES P1-1). Test: şablon render testi ilə `ems_early.js`-in head-də olduğunu və `ems_ajax_init.js`-dən əvvəl gəldiyini yoxla; JS unit yoxlaması `node` ilə (stub → drain).
- **Şəxs detalı (P1-2):** `people_directory.js openDetail` → tam render: avatar/inisial, ad, istifadəçi adı, status badge (`status_catalog`), əlaqə (yalnız `can_view_contacts`), demoqrafiya (yalnız `can_view_demographics`), üzvlüklər (rol + vahid), akademik sətirlər (ixtisas, qrup, qəbul ili, status), tədris sətirləri (fənn, qrup, dövr), əməllər (`actions` bayraqlarına görə block/unblock/grant/revoke — mövcud `runAction`), «Profil səhifəsi» linki. Konteyner: `templates/partials/ems_ui/_drawer.html` naxışı (sağdan slide-over, 375-də tam en), ESC + overlay klik bağlayır, fokus tələsi, bağla düyməsi drawer-in içində, `aria-labelledby`. Mətnlər `data-*`/JSON ilə şablondan (i18n). Dekan üçün `memberships: []` gəlir — `serialize_memberships(scope=…)` (commit olunmamış patch) tələbə üzvlüyünü (scope_unit=None) süzür → tələbə/müəllim üzvlüyü öz təşkilatındadırsa görünməlidir; patch-i düzəlt və test yaz (`apps/accounts/tests/test_people_*.py`).
- **Mobil sidebar (P2-1):** ilk yükləmədə `isMobileViewport()` isə sidebar bağlı olsun (AJAX swap-dakı davranışla eyni); `localStorage` yaddaşı desktop üçün qalsın. 768-də də yoxla.
